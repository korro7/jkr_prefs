# JKR 插件问题分析与修改方案

## 1. 报告范围

本文针对当前 `CJKR.cpp` 中的两个问题重新进行分析：

1. EDEM 启用 Contact Radius 后，Contact Radius 改变了接触力；目标是接触力始终按 Physical Radius 计算。
2. 颗粒在带粘附力的斜面上持续向下运动；导出的 `Friction` 大于重力沿斜面的分力，但并未阻止颗粒运动。

本报告只给出原因、模型选择、修改位置和验证方案，不包含代码修改。

## 2. 总体结论

两个问题应分开处理：

- Contact Radius 只能负责接触搜索和延长接触生命周期，不能进入等效半径、JKR 接触斑、法向力、切向刚度或摩擦上限的计算。
- JKR 必须使用带符号的 Physical Overlap。颗粒首次靠近但尚未发生物理接触时不应提前受力；发生物理接触后，卸载阶段可以在负 Physical Overlap 下继续保留粘附力，直到达到 JKR 断裂间隙。
- 当前导出的 `Friction` 是切向摩擦上限 `mu * hertzForce`，不是求解器实际施加的切向力，因此不能直接与 `m g sin(theta)` 比较。
- 球形颗粒即使完全没有滑移，也仍可以沿斜面滚动。要让颗粒保持静止，除了切向力平衡，还必须满足力矩平衡。
- 对当前问题，不建议首先添加 `tau * A` 型粘附剪切力。EDEM 的 JKR 摩擦上限本来就使用 JKR 法向力中的正向排斥分量；再次添加粘附剪切项容易重复计算粘附效应。
- 更物理的方案是修正实际切向力计算与诊断，同时引入带接触历史的粘附滚动阻力。

## 3. 接触模型链的前置问题

此前 EDEM 截图显示接触链同时包含：

- `JKR` 插件
- `Hertz-Mindlin (no slip)`
- `Standard Rolling Friction`

当前 JKR 插件不是单纯的附加粘附模型。它自身已经计算：

- Hertz/JKR 法向力
- 法向阻尼
- Mindlin 切向力和切向阻尼
- 滚动阻力矩

如果内置 Hertz-Mindlin 和当前插件同时执行，Hertz 法向力、切向力和阻尼会被叠加。如果内置 Standard Rolling Friction 也执行，滚动力矩同样可能被叠加。与此同时，自定义属性只统计插件内部的数值，不能表示整条 Contact Chain 的总受力。

推荐最终采用以下一种结构，不能混用：

### 推荐结构

- 当前插件作为完整 Base Contact Model。
- EDEM Contact Chain 不再额外执行内置 Hertz-Mindlin。
- 如果滚动模型在插件内实现，则不再添加内置 Standard Rolling Friction。
- 如果决定使用 EDEM 内置 Type C Rolling Friction，则插件内不再返回滚动力矩。

不先消除接触模型重复计算，后续力值对比和参数标定都不可靠。

## 4. 问题一：Contact Radius 改变接触力

### 4.1 当前代码行为

`CJKR.cpp` 第 367-373 行附近使用以下逻辑：

```cpp
double interactionRadius1 = elem1ContactRadius > OVERLAP_EPS
    ? elem1ContactRadius
    : elem1PhysicalRadius;
double interactionRadius2 = elem2ContactRadius > OVERLAP_EPS
    ? elem2ContactRadius
    : elem2PhysicalRadius;

double nEquivRadius = safeEquivalentRadius(interactionRadius1, interactionRadius2);
```

因此只要启用 Contact Radius，等效半径 `R*` 就会改变。后续以下量都会随之改变：

- JKR 四次方程系数
- JKR 接触斑半径
- 排斥分量 `hertzForce`
- 粘附分量 `cohesionForce`
- 法向刚度和阻尼
- 切向刚度
- 摩擦上限
- 滚动阻力矩

这与“Contact Radius 只负责接触检测，力按 Physical Radius 计算”的目标不一致。

### 4.2 负 Physical Overlap 被错误截断

`CJKR.cpp` 第 398-401 行附近执行：

```cpp
double positivePhysicalOverlap = clampNonNegative(normalPhysicalOverlap);
```

随后 JKR 方程使用 `positivePhysicalOverlap`。当 Contact Radius 已重叠、Physical Radius 尚未接触时：

```text
normalContactOverlap  > 0
normalPhysicalOverlap < 0
positivePhysicalOverlap = 0
```

这样一来，整个 Contact Radius 外壳范围内的负间隙都被当作 `delta = 0`。插件会在 Contact Radius 刚进入搜索范围时突然施加 `delta = 0` 对应的有限 JKR 吸引力，而不是按照真实物理间隙计算。

在已有测试中：

```text
Physical Radius = 3
Contact Radius  = 3.01
Physical Overlap = -0.009
插件法向力约为 1.13474 N
```

这不是单纯的数值精度问题，而是力学变量选择错误。颗粒可能先被不合理的远距离吸引力加速拉向几何体，形成较大压缩后再被排斥力弹开，从而表现为“接触后立刻弹飞”。

### 4.3 JKR 求根仍缺少物理解验证

当前 `selectPositiveRealRoot()` 只检查：

- 虚部足够小
- 实部有限
- 实部为正
- 在候选根中选择最大值

JKR 四次方程由原始方程平方得到，平方可能引入不满足原始方程的伪根。候选根还应代回未平方的 JKR 方程：

```text
delta = a^2 / R* - sqrt(4*pi*gamma*a/E*)
```

只有残差满足容差、且右侧根号关系成立的正实根才有效。对已经建立的 JKR 接触，应选择连续的稳定大根；超过断裂间隙后必须返回零力，不能使用 Hertz 根进行兜底。

### 4.4 推荐物理模型

建议采用接近 EDEM JKR V2 的接触状态模型。

#### Physical Radius

颗粒-颗粒：

```text
R* = R1_physical * R2_physical / (R1_physical + R2_physical)
```

颗粒-平面几何：

```text
R* = R_particle_physical
```

Contact Radius 不参与以上公式。

#### 带符号 Physical Overlap

```text
delta = normalPhysicalOverlap
```

JKR 分支必须保留 `delta < 0`。只有非粘附 Hertz 分支才使用：

```text
deltaPositive = max(delta, 0)
```

#### 每个接触的状态

建议使用 Contact Custom Property 保存 `JKRContactState`：

```text
0 = 尚未发生过物理接触
1 = 已经建立 JKR 物理接触
```

计算流程：

```text
if surfaceEnergy <= 0:
    只在 delta > 0 时计算 Hertz 力

if state == 0 and delta <= 0:
    返回零力

if delta >= 0:
    state = 1

if state == 1 and delta >= deltaCritical:
    使用带符号 delta 求稳定 JKR 接触斑和法向力

if state == 1 and delta < deltaCritical:
    state = 0
    返回零力
```

这样可以同时满足：

- 首次靠近时不会因为 Contact Radius 提前产生吸引力。
- 发生真实物理接触后，卸载过程中可以在负 Physical Overlap 下保留粘附力。
- 达到 JKR 断裂间隙后正常断开。

### 4.5 Contact Radius 的正确作用

Contact Radius 只应决定 EDEM 是否继续保留并回调这个接触。其可用间隙近似为：

颗粒-几何：

```text
availableGap = R_contact - R_physical
```

颗粒-颗粒：

```text
availableGap = (R1_contact - R1_physical)
             + (R2_contact - R2_physical)
```

要完整计算 JKR 卸载过程，应满足：

```text
availableGap >= abs(deltaCritical)
```

Contact Radius 过小会导致 EDEM 在理论断裂点之前删除接触；Contact Radius 足够大后，再继续增加它不应改变同一 Physical Overlap 和同一接触状态下的力值。

### 4.6 问题一修改位置

- 第 367-373 行：等效半径改为仅使用 Physical Radius。
- 第 398-427 行：JKR 方程改用带符号 `normalPhysicalOverlap`。
- 第 410-418 行：重新设计接触建立、保持和断裂条件。
- 第 420-443 行：增加原始 JKR 方程残差检查和断裂间隙判断。
- 第 436-440 行：接触斑数值保护只能使用 Physical Radius，不能使用 Contact Radius。
- 第 124-175 行：补全 Contact Custom Property 定义，用于保存 JKR 接触状态。

## 5. 问题二：摩擦属性很大但颗粒仍下移

### 5.1 当前 `Friction` 不是实际摩擦力

`CJKR.cpp` 第 585-587 行导出：

```cpp
FrictionDelta[0] += hertzForce * staticFriction;
```

它表示：

```text
TangentialForceLimit = mu * F_repulsive
```

真正返回给 EDEM 的实际切向力是第 528-531 行的 `newF_t`。一般情况下：

```text
abs(ActualTangentialForce) <= TangentialForceLimit
```

静摩擦是被动响应力，不会因为摩擦上限很大就自动施加满额摩擦力。它需要通过很小的相对切向位移建立切向弹簧力。

当前 `Friction` 还存在以下诊断限制：

- 只有标量大小，没有方向。
- 在 Geometry 属性上对所有接触进行累加。
- 不能区分不同颗粒或不同接触。
- 不包含 Contact Chain 中其他模型返回的切向力。
- 不能表示颗粒是粘着、滑移还是滚动。

因此，当前属性不能直接和单颗粒的 `m g sin(theta)` 比较。

### 5.2 当前摩擦上限的基本思路是合理的

EDEM 官方 JKR 模型使用 JKR 法向力中的正向排斥分量计算摩擦上限。当前：

```text
F_repulsive = hertzForce
F_limit = mu * hertzForce
```

这一思想与官方 JKR 摩擦修正一致。粘附增大接触斑时，正向排斥分量增大，因此切向摩擦上限也会增大。

所以不建议首先再加入：

```text
F_limit = mu * hertzForce + tau * contactArea
```

除非有独立的界面剪切强度实验数据，并明确需要一个与 Coulomb/JKR 摩擦不同的剪切破坏模型，否则 `tau * A` 容易重复计算粘附对摩擦的贡献。

### 5.3 颗粒可能是在滚动而不是滑动

对球形颗粒，静摩擦足够大只代表接触点不发生滑移，并不代表颗粒质心保持不动。

没有足够滚动阻力时，颗粒可以满足无滑移条件并沿斜面加速滚动。纯滚动时接触点相对切向速度接近零，但颗粒质心速度和角速度持续增加。

颗粒在斜面上保持静止必须同时满足：

```text
ActualTangentialForce = m * g * sin(theta)
RollingResistanceTorque = ActualTangentialForce * R_physical
```

对应的容量条件为：

```text
TangentialForceLimit >= m * g * sin(theta)
RollingTorqueLimit >= m * g * sin(theta) * R_physical
```

只有第一项成立时，颗粒可能不滑移但仍会滚动。

### 5.4 当前相对速度没有使用接触点速度

`CJKR.cpp` 第 323-325 行使用颗粒质心平动速度计算相对速度：

```cpp
relVel = elem1Vel - elem2Vel;
```

但 EDEM 已经向插件传入：

- `elem1ContactPointVelX/Y/Z`
- `elem2ContactPointVelX/Y/Z`

接触点速度包含平动和转动对接触点速度的共同贡献。对于滚动颗粒：

```text
质心切向速度 != 0
接触点相对切向速度约等于 0
```

当前写法会把纯滚动误判为存在切向相对速度，使切向阻尼方向和大小失真。法向及切向阻尼都应使用接触点相对速度。

### 5.5 切向刚度没有使用 JKR 接触斑

当前第 501-505 行：

```cpp
S_t = 8.0 * nEquivShearMod
    * sqrt(nEquivRadius * positivePhysicalOverlap);
```

对非粘附 Hertz 接触：

```text
a_Hertz = sqrt(R* * delta)
S_t = 8 * G* * a_Hertz
```

但 JKR 接触中，真实接触斑半径 `a_JKR` 由 JKR 方程求得。在零 Physical Overlap，甚至在卸载阶段的负 Physical Overlap 下，已经建立的 JKR 接触仍可以具有有限接触斑。

物理一致的写法应为：

```text
S_t = 8 * G* * a_contact
```

其中：

```text
a_contact = a_JKR                       粘附接触
a_contact = sqrt(R* * deltaPositive)    非粘附 Hertz 接触
```

当前代码在 `positivePhysicalOverlap == 0` 时把 `S_t` 和实际切向力清零，这会削弱刚建立或正在卸载的粘附接触的切向承载能力。

### 5.6 滑移判据应限制最终试算力

当前代码先判断弹性力 `F_t` 是否超过上限，在未超过时再叠加切向阻尼：

```text
先比较 abs(F_elastic) 与 F_limit
再计算 F_total = F_elastic + F_damping
```

这可能使最终返回力超过摩擦上限。推荐流程：

```text
F_elastic = -S_t * tangentialOverlap
F_damping = -C_t * contactPointRelativeTangentialVelocity
F_trial = F_elastic + F_damping
F_limit = mu * F_repulsive

if abs(F_trial) <= F_limit:
    F_actual = F_trial
    状态为 stick
else:
    F_actual = 与滑移方向相反、大小为 F_limit 的力
    回退 tangentialOverlap
    状态为 slip
```

滑移时的耗散力应按照 EDEM API 的 `UnsymTangentialForce` 语义正确返回，避免能量统计错误。

### 5.7 当前滚动力矩与 JKR 摩擦不一致

第 545-570 行中：

```text
elem1 torque limit 使用 abs(newF_n)
elem2 torque limit 使用 hertzForce
```

两侧使用了不同的法向载荷定义。对颗粒-几何接触，颗粒通常是 elem1，因此颗粒滚动力矩主要按 JKR 净法向力计算：

```text
F_net = F_repulsive - F_adhesive + F_damping
```

在强粘附状态下，排斥分量和粘附分量可能都很大但相互抵消，净法向力却不大。结果是：

- 导出的滑动摩擦上限很大，因为它使用 `hertzForce`。
- 颗粒滚动力矩上限可能较小，因为它使用净法向力。
- 颗粒不滑移，但仍持续滚动。

这与用户观察到的现象一致。

### 5.8 推荐的粘附滚动模型

如果目标是物理描述粘附颗粒在斜面上的起滚和滚动，推荐采用带接触历史的 JKR 粘附滚动模型，而不是瞬时施加满额 Coulomb 滚动力矩。

可采用 Dominik-Tielens 类型滚动位移模型：

```text
M_roll = -4 * F_c * (a / a0)^(3/2) * xi
```

其中：

```text
F_c  = JKR pull-off force 的大小
a    = 当前 JKR 接触斑半径
a0   = 参考 JKR 接触斑半径
xi   = 每个接触累计的滚动位移
```

当：

```text
abs(xi) <= xiCritical
```

滚动接触表现为可恢复的弹性滚动阻力；达到 `xiCritical` 后进入不可逆滚动并限制滚动力矩。`xiCritical` 是需要实验标定的新材料参数。

这一模型能够表示：

- 静止接触的小幅弹性转动
- 起滚阈值
- 粘附接触斑对滚动阻力的增强
- 起滚后的不可逆能量耗散

如果第一阶段暂时不增加 `xiCritical`，可先实现 Type C 风格的滚动弹簧，并使用统一的 JKR 有效载荷构造滚动力矩上限。但该方案属于工程近似，物理完整性低于 JKR 粘附滚动位移模型。

### 5.9 问题二修改位置

- 第 323-365 行：相对速度改为接触点速度之差。
- 第 420-443 行：把求得的 JKR 接触斑半径保存到法向和切向计算都能使用的变量中。
- 第 494-505 行：切向刚度使用实际接触斑半径。
- 第 507-526 行：对弹性与阻尼合成后的试算力执行摩擦限制。
- 第 528-538 行：正确返回实际切向力、耗散分量和回退后的切向重叠。
- 第 545-570 行：替换当前瞬时滚动力矩，或删除该段并由唯一的外部滚动模型负责。
- 第 581-588 行：将 `Friction` 明确拆分为实际切向力和摩擦上限。
- Contact Custom Property：增加接触状态和滚动位移历史。

## 6. 推荐导出的诊断量

为了在 EDEM 中判断颗粒到底是滑动还是滚动，建议至少导出：

```text
ActualTangentialForce       实际返回给 EDEM 的切向力大小
TangentialForceLimit        mu * F_repulsive
NormalRepulsiveForce        hertzForce
NormalAdhesiveForce         cohesionForce
NetNormalForce              插件返回的总法向力
ContactPointSlipSpeed       接触点相对切向速度
RollingTorqueActual         实际滚动力矩
RollingTorqueLimit          当前滚动力矩容量
ContactPatchRadius          JKR 接触斑半径
ContactState                未接触、粘着、滑移、滚动或断裂状态
```

诊断量优先使用 Contact Custom Property 保存。Geometry 累加属性可用于统计设备总载荷，但不能代替单个接触的受力诊断。

## 7. 推荐修改顺序

### 第一阶段：消除模型叠加

- 明确插件是完整 Base Model 还是附加模型。
- 推荐保留插件完整法向和切向计算，移除内置 Hertz-Mindlin 的重复贡献。
- 滚动力矩只能由一个模型负责。

### 第二阶段：修复 Contact Radius 与 JKR 法向力

- 等效半径只使用 Physical Radius。
- 使用带符号 Physical Overlap。
- 增加 JKR 接触建立和断裂状态。
- 验证四次方程根。
- Contact Radius 只保留接触，不参与力值计算。

### 第三阶段：修复实际切向力和诊断

- 使用接触点速度。
- 切向刚度使用接触斑半径。
- 对最终试算切向力执行摩擦限制。
- 分别导出实际切向力与摩擦上限。

### 第四阶段：实现粘附滚动阻力

- 优先实现带接触历史的 JKR 粘附滚动位移模型。
- 标定 `xiCritical`。
- 如需先快速验证，可使用 Type C 风格滚动弹簧作为过渡方案。

## 8. 验证方案

### 8.1 Contact Radius 独立性测试

固定：

```text
Physical Radius = 3
```

分别测试：

```text
Contact Radius = 3
Contact Radius = 3.01
Contact Radius = 3.05
```

对相同接触历史和相同 Physical Overlap，比较：

```text
ContactPatchRadius
NormalRepulsiveForce
NormalAdhesiveForce
NetNormalForce
TangentialStiffness
TangentialForceLimit
```

除接触是否仍被 EDEM 保留外，以上力学结果应在数值容差内一致。

### 8.2 Physical Radius=3、Contact Radius=3.01 专项测试

首次靠近阶段：

```text
Physical Overlap = -0.009
Contact Overlap  > 0
Contact State = 未建立
期望法向力 = 0
```

发生物理接触：

```text
Physical Overlap >= 0
Contact State 切换为已建立
```

随后卸载到：

```text
Physical Overlap = -0.009
```

如果 `-0.009 >= deltaCritical` 且 Contact Radius 范围足够，应按真实负重叠计算 JKR 吸引力；如果已经超过断裂间隙，应返回零力并清除接触状态。

### 8.3 斜面滑动/滚动分离测试

第一组锁定颗粒转动自由度：

- 若颗粒停止下移，说明原现象主要是滚动。
- 若仍持续下移，检查实际切向力、接触点滑移速度和摩擦上限。

第二组开放颗粒转动：

- 检查接触点滑移速度是否接近零。
- 检查质心速度和角速度是否满足纯滚动关系。
- 比较 `RollingTorqueActual`、`RollingTorqueLimit` 和 `m g sin(theta) R`。

第三组启用粘附滚动模型：

- 逐步增加斜面角度。
- 记录首次滑移角和首次滚动角。
- 使用实验起滚角标定 `xiCritical` 或 Type C 参数。

### 8.4 基本数值保护

每组测试必须检查：

- 不出现 NaN 或 Inf。
- 接触建立和断裂处没有非物理的巨大力跳变。
- 时间步减半后结果趋势保持一致。
- Contact Radius 增大时，同一物理状态下力值不变。
- 实际切向力不超过切向摩擦上限。
- 滚动力矩不超过当前滚动力矩上限。

## 9. 验收标准

问题一满足以下条件可认为完成：

- Contact Radius 为 `3、3.01、3.05` 时，相同 Physical Overlap 和接触历史得到相同力值。
- 首次接近但尚未物理接触时不产生提前吸引力。
- 物理接触后的卸载阶段能够在负 Physical Overlap 下正确保持 JKR 粘附力。
- 达到理论断裂间隙后平稳返回零力。

问题二满足以下条件可认为完成：

- EDEM 中可以分别观察实际切向力和摩擦上限。
- 可以使用接触点滑移速度明确区分滑动与纯滚动。
- 切向力始终满足摩擦限制。
- 颗粒静止时同时满足平动力和转动力矩平衡。
- 起滑角和起滚角可以分别标定并复现实验结果。

## 10. 参考资料

- Altair EDEM, Hertz-Mindlin with JKR Model: <https://help.altair.com/EDEM/topics/creator_tree_physics/the_hertz_mindlin_with_jkr_model_r.htm>
- Altair EDEM, Hertz-Mindlin with JKR Version 2 Model: <https://2022.help.altair.com/2022.2/EDEM/Creator/Physics/Base_Models/Hertz-Mindlin_with_JKR_V2.htm>
- Altair EDEM, Physical Radius and Contact Radius: <https://help.altair.com/2023.1/EDEM/Creator/Bulk_Material/Multi_Sphere_Pa.htm>
- Altair EDEM, Type C Rolling Friction Model: <https://help.altair.com/EDEM/topics/creator_tree_physics/the_type_c_rolling_friction_model_r.htm>
- Haervig et al., On the adhesive JKR contact and rolling models for reduced particle stiffness DEM simulations: <https://vbn.aau.dk/ws/files/305019841/accepted_HAERVIG2017_On_the_adhesive_JKR_contact_and_rolling_models_for_reduced_particle_stiffness_discrete_element_simulations.pdf>
