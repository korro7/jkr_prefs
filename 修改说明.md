# JKR 接触模型终版修改说明

## 1. 文档范围

本文说明 `main` 分支提交 `b423daa` 中 JKR 接触模型的修改内容、物理依据和验证结果。该插件实现完整接触力模型，使用时不需要再叠加 Hertz-Mindlin 等 Base Model。

本次修改主要解决两个问题：

1. EDEM 的 Contact Radius 大于 Physical Radius 时，接触壳层不应改变实际接触力。
2. 粘附颗粒在斜面上的切向摩擦和滚动阻力必须由真实接触状态和接触历史决定，不能把“摩擦上限”误当成“实际摩擦力”。

## 2. Physical Radius 与 Contact Radius 解耦

### 修改内容

- `elem1ContactRadius`、`elem2ContactRadius` 和 `normalContactOverlap` 不再进入任何本构方程。
- 粒子-粒子等效半径只由 Physical Radius 计算：

  `R* = R1 R2 / (R1 + R2)`

- 粒子-几何体接触的等效半径取粒子的 Physical Radius：

  `R* = R1`

- JKR 接触斑、法向刚度、切向刚度、摩擦上限和滚动阻力均使用 Physical Radius、Physical Overlap 或由其求得的 JKR 接触斑半径。
- Contact Radius 仍可由 EDEM 用于提前检测和保留接触，但插件在颗粒尚未发生物理接触时不产生力。

### 修改原因

Contact Radius 是 EDEM 的接触搜索壳层，不是颗粒的真实曲率半径。若把它代入等效半径或重叠量，`Physical Radius = 3`、`Contact Radius = 3.01` 时会在真实表面尚未接触前产生虚假压缩力，表现为颗粒碰到几何体后立即弹飞。

解耦后，改变 Contact Radius 只改变接触何时被 EDEM 创建和保留，不改变相同物理状态下的力和接触斑。

## 3. JKR 法向接触改为稳定分支求解

### 修改内容

使用 Physical Overlap `delta` 求解稳定 JKR 接触斑半径 `a`：

`delta = a^2 / R* - sqrt(4 pi gamma a / E*)`

其中：

- `R*`：由 Physical Radius 得到的等效半径；
- `E*`：等效杨氏模量；
- `gamma`：`jkr_prefs.txt` 中材料对的表面能；
- `a`：JKR 接触斑半径。

旧的四次方程通用求根被稳定分支上的区间扩展和 100 次二分迭代替代。接触建立后，模型允许在 Physical Overlap 略小于零时继续保留粘附接触，直到超过 JKR 理论分离点才清除接触历史。

法向力分为：

- Hertz 排斥分量：`F_H = 4 E* a^3 / (3 R*)`
- JKR 粘附分量：`F_A = 4 sqrt(pi gamma E* a^3)`
- 弹性净法向分量：`F_H - F_A`
- 最终法向力：弹性净法向力与法向阻尼力的矢量和

### 修改原因

JKR 方程可能存在多个数学根，通用求根后选根容易在临界分离附近跳支或选到非物理解。稳定分支求解保证接触斑连续，并正确表达 JKR 的拉伸卸载和有限拉脱距离。

新增 `JKR Contact State` 保存接触是否真正建立。这样，Contact Radius 壳层提前创建的接触不会凭空产生粘附力；只有发生过真实物理接触后，JKR 粘附接触才可延续到负 Physical Overlap。

## 4. 切向摩擦改为接触历史模型

### 修改内容

- 每一步将 EDEM 提供的切向物理重叠投影回当前接触切平面。
- 切向刚度使用 JKR 接触斑：`k_t = 8 G* a`。
- 试算切向力由切向弹簧力和切向阻尼力组成。
- 库仑上限使用排斥法向分量：`F_t,max = mu_s F_H`。
- 试算力未超过上限时为粘着状态；超过上限时，将实际切向力截断到上限，并回写一致的切向历史。
- 几何体自定义属性 `Friction` 累加的是实际切向力大小，不再输出摩擦容量。

### 修改原因

摩擦力不是恒等于 `mu F_N`。`mu F_N` 是可提供的最大静摩擦力，实际静摩擦力只会增长到平衡当前切向运动趋势所需的大小。旧实现仅观察一个很大的“摩擦值”，并不能证明实际施加了足够且方向正确的抗滑力。

采用切向历史后，摩擦力具有明确方向，始终反抗相对切向运动；达到上限前为静摩擦，达到上限后转为滑动摩擦。上限使用 JKR 的排斥分量 `F_H`，避免粘附与排斥相互抵消后的净法向力导致摩擦能力被低估。

## 5. 滚动阻力改为历史相关力矩

### 修改内容

- 新增三分量接触属性 `JKR Rolling Displacement`，积分接触切平面内的相对滚动角速度。
- 使用滚动弹簧和阻尼计算试算滚动力矩，并对力矩施加物理上限。
- 有表面能时，上限同时受 JKR 粘附滚动模型和库仑滚动上限约束，取二者较小值。
- 无表面能时，使用 Type C 形式的滚动刚度和库仑滚动上限。
- 对两个接触体施加大小相等、方向相反的力矩。

### 修改原因

斜面上的颗粒即使平动切向力足够，也可能通过滚动继续下坡。单独增大平动摩擦不能可靠阻止滚动。历史相关滚动力矩能区分滚动粘着与滚动滑移，并让 `rollingFriction` 对实际运动产生可验证的影响。

## 6. 数值保护和失败策略

新增以下保护：

- 检查半径、质量、模量、泊松比、重叠量、时间步和中间结果是否有限且处于有效范围。
- 恢复系数在对数计算前进行安全限制。
- 表面能为负数、非有限值或配置格式错误时拒绝加载或按安全值处理。
- JKR 求解失败、接触斑非法或超过分离点时，清零力、切向历史、滚动历史和诊断量。
- 接触斑半径不允许超过真实颗粒半径所决定的物理上限。
- 无效输入不向 EDEM 返回 `NaN` 或 `Inf`。

这些保护的目标是让异常输入安全失效，不把单个接触的数值错误放大为颗粒爆飞或整个求解器失稳。

## 7. 新增诊断属性

插件保留几何体属性 `Adhesion` 和 `Friction`，并新增 11 个接触属性：

| 属性 | 含义 |
| --- | --- |
| `JKR Contact State` | 接触状态位掩码 |
| `JKR Rolling Displacement` | 三分量滚动历史位移 |
| `JKR Actual Tangential Force` | 实际施加的切向力大小 |
| `JKR Tangential Force Limit` | 当前切向摩擦上限 |
| `JKR Repulsive Normal Force` | Hertz 排斥法向分量 |
| `JKR Adhesive Normal Force` | JKR 粘附法向分量 |
| `JKR Net Normal Force` | 含阻尼的最终法向力大小 |
| `JKR Contact Patch Radius` | JKR 接触斑半径 |
| `JKR Contact Slip Speed` | 接触点相对切向速度大小 |
| `JKR Rolling Torque` | 实际施加的滚动力矩大小 |
| `JKR Rolling Torque Limit` | 当前滚动力矩上限 |

这些量用于区分“实际力”和“力的上限”，并直接判断颗粒是在切向滑动、滚动滑移，还是处于粘着状态。

## 8. 配置文件读取改进

- 插件通过 `getPreferenceFileName()` 声明固定文件名 `jkr_prefs.txt`。
- `setup()` 使用 EDEM 传入的完整路径打开该文件。
- 支持空行和 `#` 行内注释。
- 每个非空配置行必须为 `材料A:材料B 非负表面能`。
- 文件缺失、路径为空或行格式错误时，`setup()` 返回失败并给出错误信息，不再静默使用未初始化或默认配置。
- 材料对顺序无关，`particle:blade` 与 `blade:particle` 查询结果相同。

## 9. 验证结果

终版测试程序和 EDEM 验证工程均已随仓库提交。自动验证结果为 0 项失败，覆盖：

- Release DLL 可加载且导出函数完整；
- 接口版本为 Contact Model API `V2.3.0`；
- `setup()` 能读取 `jkr_prefs.txt`；
- Physical Radius 为 `3`、Contact Radius 为 `3.01` 时，壳层接触不产生虚假力；
- 改变 Contact Radius 不改变相同 Physical Overlap 下的力和 JKR 接触斑；
- JKR 接触建立、负重叠延续和理论分离均符合预期；
- 实际切向力不超过摩擦上限；
- 几何体 `Friction` 输出实际力而非容量；
- 滚动力矩非零、方向反抗运动且不超过上限；
- 非法半径不会产生 `NaN` 或 `Inf`。

验证输出见 `verify_final_jkr_result.txt`，EDEM 示例见 `edem_real_contact_verification/111.dem`。

## 10. 当前边界

- 几何体汇总属性 `Adhesion` 和 `Friction` 在每个时间步只对名称为 `blade` 的几何体执行重置。若实际几何体名称不同，接触力计算仍正常，但这两个几何体汇总属性可能无法按预期清零和显示。
- `jkr_prefs.txt` 中的对象名称必须与 EDEM 传入插件的材料/几何类型名称完全一致，包括大小写。
- Contact Radius 可以用于扩大接触搜索范围，但不应被理解为能够扩大真实受力半径。
