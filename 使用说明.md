# EDEM JKR 插件使用说明

## 1. 适用版本和文件

当前插件按 EDEM Contact Model API `V2.3.0` 编译，并已在本机 EDEM 2024 环境完成加载和短程求解验证。

仓库中的主要文件：

| 文件 | 用途 |
| --- | --- |
| `x64/Release/jkr_src_2010_10.dll` | Release 插件 DLL |
| `jkr_prefs.txt` | 材料对表面能配置 |
| `edem_real_contact_verification/111.dem` | EDEM 验证工程 |
| `verify_final_jkr_result.txt` | 自动验证结果 |

## 2. 安装和加载

1. 关闭正在运行的 EDEM Solver，避免旧 DLL 被进程占用。
2. 将 Release DLL 放入 EDEM 的 Contact Models 插件目录。
3. 将 `jkr_prefs.txt` 放在同一个 Contact Models 目录中。
4. 在 EDEM Creator 的 Physics 页面进入相应 Interaction，例如 `Particle to Particle` 或 `Particle to Geometry`。
5. 通过 Contact Models 加载该插件，并将模型 `JKR` 加入对应接触链。
6. 本插件是完整 Base Model，实现法向力、切向力和滚动力矩。不要再叠加 Hertz-Mindlin 等另一个 Base Model，否则同一个接触会被重复计算。
7. 重新加载插件后保存 `.dem`，再启动 Solver。

插件声明的首选项文件名固定为 `jkr_prefs.txt`。EDEM 加载插件时会根据插件目录解析该文件，并把可打开的路径传给 `setup()`。只在 `.dem` 文件旁放置配置文件并不可靠；推荐始终把它与实际加载的 DLL 放在同一 Contact Models 目录。

若 DLL 在 EDEM 中重命名为 `JKR.dll`，配置文件名仍必须保持 `jkr_prefs.txt`。

## 3. `jkr_prefs.txt` 格式

每行配置一个材料对：

```text
材料A:材料B 表面能
```

当前示例：

```text
particle:particle 0.08
particle:blade 0.09
```

参数含义：

| 字段 | 含义 | 要求 |
| --- | --- | --- |
| `材料A` | 第一个粒子材料名或类型名 | 必须与 EDEM 传入名称一致 |
| `材料B` | 第二个粒子材料名、几何材料名或类型名 | 必须与 EDEM 传入名称一致 |
| `表面能` | JKR 表面能 `gamma` | SI 单位为 `J/m^2`，必须为有限的非负数 |

补充规则：

- 材料对顺序无关。
- 未配置的材料对按表面能 `0` 处理，即退化为无粘附接触。
- 支持空行。
- `#` 后面的内容作为注释忽略。
- 不要在冒号两侧加空格。
- 文件名、材料名和几何类型名建议只使用英文、数字和下划线，以减少路径和编码问题。

示例：

```text
# particle self-contact
particle:particle 0.08

# particle to inclined blade
particle:blade 0.09
```

## 4. EDEM 输入参数含义

插件本身没有额外的图形界面参数。计算使用 EDEM 材料属性、Interaction 属性、粒子尺寸以及 `jkr_prefs.txt` 中的表面能。

| 参数 | 来源 | 物理含义 | 在模型中的作用 |
| --- | --- | --- | --- |
| Physical Radius | 粒子尺寸 | 颗粒真实半径 | 计算等效半径、物理重叠、接触斑、刚度、力和力矩 |
| Contact Radius | 粒子设置 | EDEM 接触搜索壳层半径 | 只影响接触检测和保留，不改变本构力 |
| Shear Modulus | 材料属性 | 剪切模量 `G` | 计算等效杨氏模量和等效剪切模量 |
| Poisson's Ratio | 材料属性 | 泊松比 `nu` | 计算等效模量 |
| Coefficient of Restitution | Interaction | 恢复系数 `e` | 计算法向和切向阻尼比 |
| Coefficient of Static Friction | Interaction | 静摩擦系数 `mu_s` | 限制实际切向力，`F_t,max = mu_s F_H` |
| Coefficient of Rolling Friction | Interaction | 滚动摩擦系数 `mu_r` | 控制滚动刚度和滚动力矩上限 |
| Surface Energy | `jkr_prefs.txt` | JKR 表面能 `gamma` | 决定粘附力、接触斑和拉脱距离 |
| Time Step | Solver | 数值积分时间步 | 更新切向和滚动接触历史 |

### Physical Radius 与 Contact Radius

可以设置 `Contact Radius > Physical Radius`，例如 Physical Radius 为 `3`、Contact Radius 为 `3.01`。这会让 EDEM 更早创建接触搜索记录，但插件在颗粒真实表面接触前返回零力。相同 Physical Overlap 下，Contact Radius 的变化不会改变法向力、切向力或 JKR 接触斑。

不要通过增大 Contact Radius 调整粘附强度。粘附强度应通过表面能、真实粒径和材料模量标定。

### 表面能

表面能越大，通常会增大 JKR 接触斑、粘附力、拉脱距离和粘附条件下的摩擦/滚动承载能力。建议从实验或已验证文献值开始标定，不要仅为阻止斜面下滑而无限增大表面能。

### 静摩擦系数

该参数给出切向力上限，不是每一步固定施加的摩擦力。实际切向力由接触历史计算，满足：

`|F_t| <= mu_s F_H`

判断是否滑动时，应同时查看 `JKR Actual Tangential Force`、`JKR Tangential Force Limit` 和 `JKR Contact Slip Speed`。

### 滚动摩擦系数

斜面颗粒可能不是平移滑动，而是在滚动。`rollingFriction` 控制抵抗相对滚动的力矩。判断滚动状态时，应查看 `JKR Rolling Torque`、`JKR Rolling Torque Limit` 和 `JKR Contact State`。

## 5. 输出属性含义

### 几何体属性

| 属性 | 单位 | 含义 |
| --- | --- | --- |
| `Adhesion` | N | 当前时间步作用于几何体的 JKR 粘附法向分量总和 |
| `Friction` | N | 当前时间步作用于几何体的实际切向力大小总和 |

`Friction` 不是 `mu_s F_H` 上限。如果多个颗粒同时接触几何体，它是这些接触实际切向力大小的累加值，不是带方向的合力。

当前代码只对名称为 `blade` 的几何体重置这两个汇总属性。若需要在 Analyst 中可靠查看它们，几何体名称应为 `blade`。

### 接触属性

| 属性 | 单位 | 含义和判断方法 |
| --- | --- | --- |
| `JKR Contact State` | 无量纲 | 位掩码：`1` 接触已建立，`2` 切向滑动，`4` 滚动滑移 |
| `JKR Rolling Displacement` | m | 三分量滚动历史变量，主要用于模型内部积分 |
| `JKR Actual Tangential Force` | N | 当前接触实际施加的切向力大小 |
| `JKR Tangential Force Limit` | N | 当前库仑切向力上限 `mu_s F_H` |
| `JKR Repulsive Normal Force` | N | Hertz 排斥法向分量 `F_H` |
| `JKR Adhesive Normal Force` | N | JKR 粘附法向分量 `F_A` |
| `JKR Net Normal Force` | N | 含阻尼的最终法向力矢量大小 |
| `JKR Contact Patch Radius` | m | 当前 JKR 接触斑半径 `a` |
| `JKR Contact Slip Speed` | m/s | 接触点相对切向速度大小 |
| `JKR Rolling Torque` | N m | 当前实际滚动力矩大小 |
| `JKR Rolling Torque Limit` | N m | 当前滚动力矩上限 |

`JKR Contact State` 的常见值：

| 值 | 状态 |
| --- | --- |
| `0` | 未建立或已经分离 |
| `1` | 已接触，切向和滚动均未滑移 |
| `3` | 已接触并发生切向滑动，即 `1 + 2` |
| `5` | 已接触并发生滚动滑移，即 `1 + 4` |
| `7` | 已接触，同时发生切向滑动和滚动滑移 |

## 6. 在 Analyst 中判断斜面运动

建议同时输出下列量，不要只看几何体 `Friction`：

1. 颗粒质心速度及沿斜面方向的速度分量。
2. 颗粒角速度，用于区分平移滑动和滚动。
3. `JKR Contact Slip Speed`。
4. `JKR Actual Tangential Force` 与 `JKR Tangential Force Limit`。
5. `JKR Rolling Torque` 与 `JKR Rolling Torque Limit`。
6. `JKR Contact State`。

判断原则：

- `Actual Tangential Force < Limit` 且 Slip Speed 接近零：切向粘着。
- `Actual Tangential Force` 接近 `Limit` 且 State 包含位 `2`：切向滑动。
- 平移 Slip Speed 很小但颗粒仍转动下坡：重点检查滚动力矩。
- `Rolling Torque` 接近 `Rolling Torque Limit` 且 State 包含位 `4`：滚动滑移。
- 几何体 `Friction` 大于重力沿斜面分力，不等于颗粒必然静止；还需检查力的方向、瞬态惯性、滚动和是否存在多个接触累加。

## 7. 推荐验证流程

1. 先使用仓库中的 `edem_real_contact_verification/111.dem` 确认插件可以加载和运行。
2. 在单颗粒-平面模型中使用较小时间步，确认 Physical Radius 接触前所有力为零。
3. 分别用相同 Physical Radius 和不同 Contact Radius 运行，比较 `JKR Contact Patch Radius` 与法向力，应保持一致。
4. 在水平面静置测试中检查颗粒能否稳定达到静力平衡。
5. 再增加斜面角度，分别观察切向滑动和滚动滑移的临界状态。
6. 最后再进行表面能、静摩擦系数和滚动摩擦系数标定。

已验证案例中，Physical Radius 为 `3`、Contact Radius 为 `3.01` 时，初始壳层接触力为 `0`；全部自动检查结果见 `verify_final_jkr_result.txt`。

## 8. 常见故障

### 插件列表中没有 JKR

- 确认 EDEM 加载的是 x64 Release DLL。
- 确认 DLL 位于当前 EDEM 配置的 Contact Models 目录。
- 关闭 EDEM 后替换 DLL，再重新启动，避免旧 DLL 仍被占用。
- 确认没有同时加载同名旧版本 DLL。

### 提示无法打开配置文件

- 确认文件名严格为 `jkr_prefs.txt`。
- 确认配置文件与实际加载的 DLL 位于同一 Contact Models 目录。
- 确认 EDEM 进程对该目录有读取权限。
- 配置文件缺失时插件会加载失败，不会自动使用默认表面能。

### 提示配置行无效

正确格式为：

```text
materialA:materialB 0.08
```

表面能必须是有限的非负数，材料对中必须存在冒号。

### Contact Radius 打开后仍出现弹飞

- 确认 EDEM 实际加载的是本仓库终版 DLL，而不是旧 DLL。
- 在 Analyst 中检查 `JKR Contact State` 和 `JKR Repulsive Normal Force`；Physical Overlap 为负且状态为 `0` 时，力应为零。
- 检查是否在接触链中又叠加了另一个 Base Model。
- 检查时间步、材料刚度、密度和恢复系数，过大时间步仍可能造成普通 DEM 数值不稳定。

### `Friction` 很大但颗粒仍下滑

- `Friction` 是几何体上多个接触的标量累加，不能直接与单颗粒重力分力比较。
- 查看单个接触的实际切向力、上限、滑移速度和状态位。
- 查看颗粒角速度和滚动力矩，确认是平移滑动还是滚动。
- 确认几何体名称为 `blade`，否则汇总属性可能未按时间步正确重置。
