# JKR Contact Radius 问题分析报告

本次仅进行了代码分析，没有修改现有业务逻辑代码。

## 1. 问题现象

在 EDEM 中：

- 使用 `Physical Radius` 时，模型运行正常
- 打开 `Contact Radius` 后，颗粒与几何体一接触就立刻被弹飞

当前判断是：问题主要出在 `calculateForce()` 内部对 `Contact Radius` / `Physical Radius` 的使用假设不一致，导致法向力求解异常放大。

## 2. 核心结论

这份接触模型代码本身默认：

- `Physical Radius == Contact Radius`

直接证据见：

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:283)

```cpp
// This contact model assumes that the Physical and the Contact radius are the same
```

因此，一旦在 EDEM 中启用了 `Contact Radius`，而 `Contact Radius` 与 `Physical Radius` 不再一致，当前模型的法向接触、JKR 根求解、阻尼和切向刚度计算都会偏离原始假设，最终导致碰撞瞬间出现过大的排斥力。

## 3. 代码层面的具体原因

### 3.1 模型拿到了 Contact Radius 相关输入，但几乎没有正确使用

`calculateForce()` 接口中传入了：

- `elem1ContactRadius`
- `elem1PhysicalRadius`
- `elem2ContactRadius`
- `elem2PhysicalRadius`
- `normalContactOverlap`
- `normalPhysicalOverlap`

对应位置见：

- [CJKR.h](D:/wzh_pro/jkr_src_2010_10/CJKR.h:110)
- [CJKR.h](D:/wzh_pro/jkr_src_2010_10/CJKR.h:144)
- [CJKR.h](D:/wzh_pro/jkr_src_2010_10/CJKR.h:173)
- [CJKR.h](D:/wzh_pro/jkr_src_2010_10/CJKR.h:174)

但在真正的力学计算里，代码主要使用的是：

- `elem1PhysicalRadius`
- `elem2PhysicalRadius`
- `normalPhysicalOverlap`

典型位置：

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:330)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:371)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:381)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:400)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:423)

这意味着：

- EDEM 已经根据 `Contact Radius` 判定“发生接触”
- 但你的模型仍然按“实体半径已经真实压入”来计算

这两套定义在 `Contact Radius != Physical Radius` 时是不一致的。

### 3.2 `normalContactOverlap` 被传入，但没有参与核心法向力求解

当前模型虽然收到了：

- `normalContactOverlap`
- `normalPhysicalOverlap`

但真正进入 Hertz/JKR 公式的是 `normalPhysicalOverlap`。

这会导致一个典型问题：

- `Contact Radius` 打开后，EDEM 会更早触发接触
- 此时 `normalContactOverlap` 可能已经大于 0
- 但 `normalPhysicalOverlap` 仍然很小，甚至可能小于等于 0

于是模型会在“不适合直接套 Hertz/JKR 压入公式”的区间里开始计算。

### 3.3 JKR 接触半径 `nContactRadius` 的根选择方式不安全

关键代码：

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:368)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:375)

```cpp
double a[5], rr[4], ri[4];
quart(a, rr, ri);
double nContactRadius = rr[3];
```

问题在于：

- 四次方程求出 4 个根后
- 代码直接固定拿 `rr[3]`
- 没有检查：
  - 是否为实根
  - 虚部是否接近 0
  - 是否为正根
  - 是否为物理上合理的根

而 [quarticroot.h](D:/wzh_pro/jkr_src_2010_10/quarticroot.h:10) 到 [quarticroot.h](D:/wzh_pro/jkr_src_2010_10/quarticroot.h:67) 只是计算根，并不保证 `rr[3]` 一定就是正确的物理解。

这在 `Contact Radius` 打开后尤其危险，因为方程输入状态更容易落入不稳定区间，导致：

- `nContactRadius` 取值异常
- `hertzForce` 突然过大
- 接触瞬间把颗粒直接弹飞

### 3.4 法向力对 `nContactRadius` 高度敏感

见：

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:377)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:378)

```cpp
cohesionForce = 4.0 * sqrt(PI * nSurfaceEnergy * nEquivYoungsMod) * pow(nContactRadius, 1.5);
hertzForce = 4.0 / 3.0 * nEquivYoungsMod / nEquivRadius * pow(nContactRadius, 3.0);
```

特别是：

- `hertzForce ~ nContactRadius^3`

所以只要 `nContactRadius` 稍微选大一点，法向力就会成倍放大。

### 3.5 阻尼和切向刚度也依赖 `normalPhysicalOverlap`

关键代码：

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:400)
- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:423)

```cpp
double S_n = 2.0 * nEquivYoungsMod * sqrt(nEquivRadius * normalPhysicalOverlap);
double S_t = 8.0 * nEquivShearMod * sqrt(nEquivRadius * normalPhysicalOverlap);
```

如果：

- 已经由 `Contact Radius` 触发接触
- 但 `normalPhysicalOverlap <= 0` 或非常小

那么这里会出现：

- 根号项数值不稳定
- 阻尼异常
- 切向刚度异常

这些都会进一步加剧“碰撞后立刻弹飞”的现象。

### 3.6 等效半径始终按 Physical Radius 计算

见：

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:330)

```cpp
double nEquivRadius = elem1PhysicalRadius * elem2PhysicalRadius /
    (elem1PhysicalRadius + elem2PhysicalRadius);
```

说明整套接触几何仍然是按 `Physical Radius` 构造的。

而在 EDEM 开启 `Contact Radius` 后：

- 接触触发时机
- 接触点位置
- 重叠定义

都可能不再与纯 `Physical Radius` 一致。

因此模型几何假设与 EDEM 的接触机制不再一致。

## 4. 为什么 Physical Radius 正常而 Contact Radius 异常

### 4.1 仅用 Physical Radius 时

此时：

- EDEM 的接触判断
- 接触点位置
- 重叠量定义
- Hertz / JKR 公式假设

大体是一致的，所以模型能够正常运行。

### 4.2 开启 Contact Radius 时

此时：

- EDEM 更早认定接触发生
- `normalContactOverlap` 与 `normalPhysicalOverlap` 会分离
- 当前模型没有处理这种分离

因此模型会把一个“壳层先接触、实体未充分压入”的状态，错误地当成“实体已经进入标准 Hertz/JKR 压入区间”来处理，最终导致法向力求解失真。

## 5. 最可能的直接致因

按优先级排序，最关键的是以下两点：

### 5.1 设计前提被打破

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:283)

模型从设计上就不支持：

- `Physical Radius != Contact Radius`

### 5.2 JKR 根选择不安全

- [CJKR.cpp](D:/wzh_pro/jkr_src_2010_10/CJKR.cpp:375)

直接使用 `rr[3]`，没有做物理有效性筛选，是数值爆炸的重要风险点。

## 6. 后续如何修改

以下为建议的修改路线，按“先稳住，再完善”的顺序进行。

### 6.1 第一阶段：先加数值保护

目标：

- 避免一打开 `Contact Radius` 就立刻数值爆炸

建议处理：

1. 对 `normalPhysicalOverlap <= 0` 做保护
2. 对所有 `sqrt(...)` 输入做下限裁剪
3. 对 `nContactRadius` 做合法性检查：
   - 必须为实根
   - 必须大于 0
   - 必须在可接受范围内

### 6.2 第二阶段：明确 overlap 的物理定义

目标：

- 让模型逻辑与 EDEM 的 `Contact Radius` 模式一致

建议二选一：

#### 方案 A：明确限制模型只支持 `Physical Radius == Contact Radius`

做法：

- 在 `calculateForce()` 或更早阶段检测：
  - `elem1ContactRadius != elem1PhysicalRadius`
  - `elem2ContactRadius != elem2PhysicalRadius`
- 一旦不一致，直接报错并拒绝运行

优点：

- 修改最小
- 行为最可控

缺点：

- 不能使用 EDEM 的 `Contact Radius` 功能

#### 方案 B：真正支持 Contact Radius

做法：

- 重新定义接触触发与力学压入之间的关系
- 区分：
  - 哪个量用于接触判定
  - 哪个量用于 Hertz / JKR 压入

优点：

- 能真正兼容 EDEM 的 `Contact Radius`

缺点：

- 修改量更大
- 需要重新核对物理模型一致性

### 6.3 第三阶段：重写 JKR 根选择逻辑

当前实现：

- 固定取 `rr[3]`

建议修改为：

1. 遍历 `rr[i]` 和 `ri[i]`
2. 筛选 `abs(ri[i]) < eps` 的根
3. 在实根中筛选 `rr[i] > 0`
4. 根据 JKR 物理意义选出合理根

至少不要再使用“固定数组下标”这种方式选根。

### 6.4 第四阶段：重新审查等效半径与接触半径的用法

当前：

- `nEquivRadius` 永远使用 `PhysicalRadius`

后续需要明确：

1. Hertz / JKR 压入公式是否仍然只基于 `PhysicalRadius`
2. `ContactRadius` 是否只用于接触触发
3. 是否需要引入与 `ContactRadius` 一致的辅助几何定义

### 6.5 第五阶段：增加调试输出

建议在调试版中记录以下量：

- `elem1ContactRadius`
- `elem1PhysicalRadius`
- `elem2ContactRadius`
- `elem2PhysicalRadius`
- `normalContactOverlap`
- `normalPhysicalOverlap`
- `nContactRadius`
- `hertzForce`
- `cohesionForce`

这样可以很快确认“颗粒弹飞”是不是某一步法向力突然跳大。

## 7. 推荐的实施顺序

如果目标是“先让模型稳定可运行”，建议：

1. 先做方案 A
2. 明确限制只支持 `Physical Radius == Contact Radius`
3. 同时补上 `nContactRadius` 的根合法性筛选
4. 加入最基本的数值保护

如果目标是“必须支持 Contact Radius”，建议：

1. 先补数值保护
2. 再重构 `normalContactOverlap` / `normalPhysicalOverlap` 的使用逻辑
3. 最后重写 JKR 根筛选与等效半径计算逻辑

## 8. 最终建议

当前最稳妥的判断是：

- 这份 JKR 模型不是简单“参数没调好”
- 而是**代码逻辑本身没有为 Contact Radius 模式做好准备**

因此，如果继续直接在现有代码上使用 `Contact Radius`，即使偶尔调通，也很可能存在数值不稳定风险。

建议优先处理的两个问题是：

1. 去掉固定 `rr[3]` 的根选择方式
2. 明确模型是否真正支持 `Physical Radius != Contact Radius`

